import React from 'react';
import Button from '../components/common/Button';

const LoginPage = () => {
  return (
    <div>
      로그인 페이지
      <Button>로그인</Button>
      <Button gray>로그인</Button>
      <Button white>로그인</Button>
      <Button closed>로그인</Button>
      <Button full>로그인</Button>
    </div>
  );
};

export default LoginPage;
